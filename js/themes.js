// Central place to define the default typography stack for the editor and MJML output.
// Update this value to change the default font everywhere in one place.
export const DEFAULT_FONT_FAMILY =
  "Aptos, 'Inter', 'Segoe UI', system-ui, -apple-system, BlinkMacSystemFont, 'Helvetica Neue', Arial, sans-serif";

// Simple theming tokens (can be adjusted later or made user-editable)
export const Theme = {
  brandColor: '#086bd4',
  fontFamily: DEFAULT_FONT_FAMILY,
  spacing: '16px',
  logoUrl: 'assets/default-logo.svg'
};

export default Theme;
